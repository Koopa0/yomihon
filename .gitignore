bin/
tmp/
dist/
/yomihon
/yomihon.exe
.env
.DS_Store
# Frontend tools install under .github/; ignore dependency trees at any depth.
# The product build and Go package graph need no Node.
node_modules/
# This is a private, single-user tool. Its process, governance, agent, and
# design docs live on the maintainer's machine, not in history: a clean clone
# receives only the code, its build inputs, and the redistribution notices.
# tests/ holds local harness self-tests; yomihon's Go tests live in
# internal/*_test.go.
.claude/
.agents/
.codex/
.lsp.json
tests/
/AGENTS.md
/CLAUDE.md
/CONTRIBUTING.md
/ENGINEERING_STANDARD.md
/ENGINEERING_STANDARD.sha256
/PROJECT_PROFILE.md
/SECURITY.md
/docs/
/.github/CODEOWNERS
/.github/PULL_REQUEST_TEMPLATE.md
