bin/
tmp/
dist/
/yomihon
/yomihon.exe
.env
.DS_Store
# Frontend tools install under .github/; ignore dependency trees at any depth.
# The product build and Go package graph need no Node.
node_modules/
# Optional maintainer harness internals live on disk, never in this product
# repository. The public control plane itself (AGENTS.md and CLAUDE.md) is
# tracked so a clean clone receives the same authority and product walls.
# tests/ holds only local harness self-tests; yomihon's Go tests live in
# internal/*_test.go.
.claude/
.agents/
.codex/
tests/
