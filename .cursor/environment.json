{
  "name": "yomihon",
  "install": "bash .cursor/install.sh",
  "terminals": [
    {
      "name": "yomihon serve",
      "command": "test -x bin/yomihon || go build -o bin/yomihon ./cmd/yomihon; exec bin/yomihon serve examples/vault",
      "description": "Serves the bundled example vault on http://127.0.0.1:9610. Rebuilds the binary first if it is missing."
    }
  ],
  "ports": [
    {
      "name": "yomihon reader",
      "port": 9610
    }
  ]
}
