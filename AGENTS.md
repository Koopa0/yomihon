# yomihon v2 — agent 入口

本 repo 採薄 harness：repo 特有事實全部在 `CLAUDE.md`（一頁），先讀它。

Go 編碼規則不在本 repo 內：完整標準見 `~/go/src/github.com/koopa0/go-spec/AGENTS.md`。

動 renderer / graph / search 前，`docs/vault-model.md` 是必修課。
