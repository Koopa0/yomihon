# kurodo — agent entry point

This repo uses a thin harness: every repo-specific fact lives in `CLAUDE.md` (one page) — read it first.

Go coding rules are not kept in this repo: for the full standard, see `~/go/src/github.com/koopa0/go-spec/AGENTS.md`.

Before touching renderer / graph / search, `docs/vault-model.md` is required reading.
