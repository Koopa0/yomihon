// Package agent implements privacy-gated, agent-facing search. It owns the
// action snapshot, lexical and semantic fusion, and the frozen CLI contract.
// Ordinary human search remains in the parent search package.
package agent
